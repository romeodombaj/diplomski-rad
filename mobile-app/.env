# LAN IP of the dev PC running the backend (backend/.env.development -> PORT=5001).
# This file is COMMITTED on purpose: EXPO_PUBLIC_* values are inlined into the JS
# bundle at build time, so the Mac that builds the iOS app needs it from git.
EXPO_PUBLIC_API_URL=http://192.168.88.219:5001
